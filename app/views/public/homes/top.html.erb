<!--誰でも見れるカレンダー-->
<div class='row'>
    <%= render 'layouts/public_sidebar' %>
    <div class='col-md-9 offset-md-1 mt-3'>
      <%= render partial: 'layouts/flash' %>
      <%= month_calendar(partial: 'simple_calendar/public_month_calendar') do |date| %>
        <%= date.strftime("%d") %>
      <% end %>
    </div>
  </div>
