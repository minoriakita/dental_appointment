<nav class="navbar navbar-expand-lg navbar-light border-bottom border-dark" >
  <div class="container">
    <a class="navbar-brand" href="/"><span><i class="fas fa-tooth mr-2"></i>Dental apo</span></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ml-auto">
        <div class="menu">
          <ul class="navbar-nav">
            <li class="nav-item">
              <!--if トップページならログアウトリンク表示-->
              <%= link_to destroy_admin_session_path, method: :delete, class: "nav-link" do %><i class="fas fa-sign-out-alt"></i>Log out<% end %>
            </li>
          </ul>
        </div>
        <div>
          <%= form_with url: search_admin_patients_path, method: :get, local: true, class: 'form-inline my-2 my-lg-0 input-group' do |f| %>
            <%= f.text_field :keyword, value: @keyword, placeholder: 'Search', class: 'form-control', 'aria-describedby': 'search-submit' %>
            <div class="input-group-append">
              <%= f.submit "検索", class: 'btn btn-outline-secondary', id: 'search-submit' %>
            </div>
          <% end %>
        </div>
      </ul>
    </div>
  </div>
</nav>
