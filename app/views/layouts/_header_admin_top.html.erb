<nav class="navbar navbar-light px-0" >
  <div class="container-fluid">
    <a class="navbar-brand" href="/admin/top"><%= image_tag 'dentalapo2.png', :size => '140x30' %></a>
      <div class="menu mx-2">
        <ul class="navbar-nav">
          <li class="nav-item">
            <!--トップページのヘッダーはログアウトリンク表示-->
            <%= link_to destroy_admin_session_path, method: :delete, class: "nav-link" do %>ログアウト<i class="fas fa-sign-out-alt ml-2"></i><% end %>
          </li>
        </ul>
      </div>
      <div>
        <%= form_with url: search_admin_patients_path, method: :get, local: true, class: 'form-inline my-0 input-group' do |f| %>
          <%= f.text_field :keyword, value: @keyword, placeholder: 'Search', required: 'required', class: 'form-control', 'aria-describedby': 'search-submit' %>
          <div class="input-group-append">
            <%= button_tag type: "submit", class: "btn btn-outline-secondary", id: 'search-submit' do %>
              <i class="fas fa-search"></i>
            <% end %>
          </div>
        <% end %>
      </div>
  </div>
</nav>