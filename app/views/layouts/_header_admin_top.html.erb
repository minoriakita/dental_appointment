<nav class="navbar navbar-light px-0" >
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Dental apo<span><i class="fas fa-tooth mr-2"></i></span></a>
      <div class="menu mx-2">
        <ul class="navbar-nav">
          <li class="nav-item">
            <!--トップページのヘッダーはログアウトリンク表示-->
            <%= link_to destroy_admin_session_path, method: :delete, class: "nav-link" do %>Log out<i class="fas fa-sign-out-alt"></i><% end %>
          </li>
        </ul>
      </div>
      <div>
        <%= form_with url: search_admin_patients_path, method: :get, local: true, class: 'form-inline my-0 input-group' do |f| %>
          <%= f.text_field :keyword, value: @keyword, placeholder: 'Search', class: 'form-control', 'aria-describedby': 'search-submit' %>
          <div class="input-group-append">
            <%= button_tag type: "submit", class: "btn btn-outline-secondary", id: 'search-submit' do %>
              検索<i class="fas fa-search"></i>
            <% end %>
          </div>
        <% end %>
      </div>
  </div>
</nav>